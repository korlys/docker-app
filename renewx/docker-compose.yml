version: '3.9'

services:
  server:
    image: hanhongyong/ms365-e5-renew-x:latest
    container_name: renewx
    environment:
      - USER_UID=1000
      - USER_GID=1000
    restart: always
    privileged: true
    volumes:
      - /etc/localtime:/etc/localtime
    ports:
      - "9060:1066"

